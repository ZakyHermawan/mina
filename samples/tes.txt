{ var a : integer
  var b : integer
  var c : boolean;

  get (a)
  b := 0
  if a > b then
     c := true
  else
     c := false
  end if
  { var a : integer;
    a := 5
    if c then
       repeat
          b := b+1
          put(b, skip)
       until b >= a
    end if
  }
  a := {; ; 1+1}
  put (a, skip)
}
