{
	var a: integer
	proc empty() {
	;
		put(13, skip)
	}
	integer func someFunc(a: boolean, b: integer) {; return b}
	proc something(x: integer, y: boolean, z: integer) {
	;
		put(x, skip)
	}

;
	a := 5
	if a = 5 then
		a := 1
	else
		a := 2
	end if
	put(a, skip)
	put(9123, skip)
	put(true, skip)
	a := someFunc(false, 100)
	empty()
	something(a)
	something(19)
}
