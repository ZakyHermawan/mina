{
	var a: integer
	proc empty() {
	;
		put(13, skip)
	}
	proc something(x: integer) {
	;
		put(x, skip)
	}
;
	a := 5
	if a = 5 then
		a := 1
	else
		a := 2
	end if
	put(a, skip)
	put(9123, skip)
	put(true, skip)
	empty()
	something(a)
	something(19)
}
